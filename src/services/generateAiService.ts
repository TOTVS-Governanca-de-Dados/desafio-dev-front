/* eslint-disable @typescript-eslint/no-unused-vars */
export const IGenerativeAiService = {
  generateContent: async (prompt: string) => {
    throw new Error("generateContent method must be implemenented");
  },
};
